name: My First Workflow

on: push

jobs:
    say-hello:
        runs-on: ubuntu-latest

        steps:
            - name: Print message
              run: echo "Hello, Github Actions!"
            - name: Print name
              run: echo "Sumiti"
            - name: Print Bye
              run: echo "bye github action"